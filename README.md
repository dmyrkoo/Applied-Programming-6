# Лабораторна робота №6  
## Варіант 8 — Рухомий склад залізничного транспорту

---

## 📌 Опис проєкту

Цей проєкт є реалізацією **лабораторної роботи №6** з дисципліни *Об’єктно-орієнтоване програмування*  
**Варіант 8 — «Рухомий склад залізничного транспорту»**.

Метою роботи є:
- закріплення принципів **ООП**;
- використання **патерну проєктування «Команда» (Command)**;
- робота з **Maven**;
- написання та запуск **модульних тестів**.

Проєкт реалізує модель рухомого складу залізничного транспорту (поїзди, вагони тощо) та дозволяє виконувати дії над об’єктами за допомогою команд.

---

## 🧩 Використані технології

- **Java JDK 17+**
- **Maven 3.6+**
- **JUnit** (для тестування)
- **ООП** (інкапсуляція, наслідування, поліморфізм)
- **Патерн Command**

---

## 📂 Структура проєкту

```text
.
├── src/
│   ├── main/
│   │   └── java/        # Основний Java-код
│   └── test/
│       └── java/        # Модульні тести
│
├── potiag_data.txt      # Вхідні / тестові дані
├── pom.xml              # Maven-конфігурація
├── .gitignore           # Ігноровані файли (IDE, target тощо)
└── README.md            # Опис проєкту
Добре 🙂 Давай зробимо **README.md** більш повним, структурованим і зрозумілим — так, щоб **викладачу було легко перевіряти**, а **тобі — пояснювати проєкт**.

Нижче — **готовий варіант README.md**, який можна просто скопіювати в файл.

---

````markdown
# Лабораторна робота №6  
## Варіант 8 — Рухомий склад залізничного транспорту

---

## 📌 Опис проєкту

Цей проєкт є реалізацією **лабораторної роботи №6** з дисципліни *Об’єктно-орієнтоване програмування*  
**Варіант 8 — «Рухомий склад залізничного транспорту»**.

Метою роботи є:
- закріплення принципів **ООП**;
- використання **патерну проєктування «Команда» (Command)**;
- робота з **Maven**;
- написання та запуск **модульних тестів**.

Проєкт реалізує модель рухомого складу залізничного транспорту (поїзди, вагони тощо) та дозволяє виконувати дії над об’єктами за допомогою команд.

---

## 🧩 Використані технології

- **Java JDK 17+**
- **Maven 3.6+**
- **ООП** (інкапсуляція, наслідування, поліморфізм)
- **Патерн Command**

---

## 📂 Структура проєкту

```text
.
├── src/
│   ├── main/
│   │   └── java/        # Основний Java-код
│   └── test/
│       └── java/        # Модульні тести
│
├── potiag_data.txt      # Вхідні / тестові дані
├── pom.xml              # Maven-конфігурація
├── .gitignore           # Ігноровані файли (IDE, target тощо)
└── README.md            # Опис проєкту
````

---

## 📄 Опис основних файлів

* **`pom.xml`**
  Містить:

  * версію Java;
  * залежності (JUnit);
  * налаштування збірки Maven.
    **Обов’язковий файл для перевірки викладачем.**

* **`potiag_data.txt`**
  Файл з тестовими даними для:

  * демонстрації роботи програми;
  * автоматичних тестів.

* **`.gitignore`**
  Виключає з репозиторію:

  * папку `target/`;
  * файли IDE (IntelliJ IDEA, Eclipse);
  * логи.

---

## ▶️ Швидкий старт

### 1️⃣ Збірка проєкту

```bash
mvn clean package
```

У результаті буде створено `.jar` файл у папці `target/`.

---

### 2️⃣ Запуск програми

```bash
java -jar target/your-artifact-name.jar
```

> ⚠️ Замініть `your-artifact-name.jar` на реальну назву файлу з папки `target`.

---

## 🧠 Архітектура та підхід

### 🔹 Об’єктно-орієнтоване програмування

У проєкті використано:

* **класи та об’єкти** для представлення рухомого складу;
* **наслідування** для спільної логіки;
* **інкапсуляцію** для захисту даних;
* **поліморфізм** для гнучкої роботи з командами.

---

### 🔹 Патерн «Команда» (Command)

Патерн **Command** дозволяє:

* інкапсулювати запит як об’єкт;
* відокремити логіку виклику дії від її реалізації;
* легко додавати нові команди без зміни існуючого коду.

Це робить програму:

* гнучкішою;
* легшою для розширення;
* зручнішою для тестування.

---

## 📝 Примітка 

Файли:

* **`pom.xml`**
* **`potiag_data.txt`**

дозволяють повністю зібрати, запустити та перевірити проєкт без додаткових налаштувань.
Файл **`.gitignore`** залишено для прозорості структури репозиторію.

---

**Автор:** *Петрунів Дмитро*
**Лабораторна робота №6, Варіант 8*

```

---


```
